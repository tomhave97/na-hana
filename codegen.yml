schema: https://api-eu-central-1.graphcms.com/v2/${GRAPH_CMS_API_KEY_PROD}/master
documents: graphql/**/*.graphql
generates:
  generated/schema.ts:
    config:
      documentMode: documentNode
    plugins:
      - typescript
      - typescript-operations
      - typescript-vue-urql